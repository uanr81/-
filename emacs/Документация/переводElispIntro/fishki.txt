 ;;; Emacs Load Path
     (setq load-path (cons "~/emacs" load-path))
код надолго  ‘.emacs’ ‘load’ ‘site-init.el’
 (let ((ПЕРЕМЕННАЯ ЗНАЧЕНИЕ)
           (ПЕРЕМЕННАЯ ЗНАЧЕНИЕ)
           …)
       ТЕЛО…)
<TAB> автоматически выравнивает строку в которой находится курсор
‘M-C-\’ команда выравнивания области текста 
