файл Doc /usr/share/emacs/24.5/etc/ краткая справка по функции

Вы можете перевести обычный код в байт-компилированный, запустив команду для компиляции, например, 
byte-compile-file.

substring как своего рода "разделитель атома"

Vfill-column для переменных

(fn)Ffile-system-info для функций

set

setq

