re-search-forward is an interactive built-in function in `C source
code'.

(re-search-forward REGEXP &optional BOUND NOERROR COUNT)

Поиск вперед с точки для регулярного выражения REGEXP.
Задайте точку в конце найденного события и точку возврата.
Необязательный второй аргумент ограничивает поиск; это позиция буфера.
Соответствие не должно распространяться после этой позиции.
Необязательный третий аргумент, если t, означает, что если fail просто
возвращает nil (без ошибок).
  Если не ноль, а не t, перейдите к пределу поиска и возвратите нуль.
  Необязательный четвертый аргумент повторяется count--search для
  последовательных случаев.

Чувствительность к регистру поиска определяется значением переменной
`case-fold-search', which see.

См. Также функции `match-beginning', `match-end', `match-string',
and `replace-match'.

match-string is a compiled Lisp function in `subr.el'.

(match-string NUM &optional STRING)

Возвращает строку текста, соответствующую последнему поиску.
NUM задает выражение в скобках в последнем регулярном выражении.
 Значение равно нулю, если NUMth пара не соответствует, или было меньше
 NUM пар. Zero означает весь текст, сопоставляемый целым регулярным выражением
 или цельной строкой.
STRING должен быть указан, если последний поиск был `string-match' on STRING.
Если STRING nil, текущий буфер должен быть тем же самым буфером, в котором
выполнялся поиск / совпадение.