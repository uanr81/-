#include<stdio.h>
#include<ctype.h>
#define STOP '#'

int main (void)
{
  int num ;
  int cont_chet = 0;
  int cont_nechet = 0;
  int sum_chet = 0;
  int sum_nechet = 0;
  printf("Введите текст\n");
  while ((scanf("%d", &i) == 1))
    {
      if(isspace(
      if(i !== 0)

	break
      printf("Введенный символ: %c , его десятичный код ASCII: %d -- ", c, c);
      while(++i < 9)
	printf(" %c: %d ;", c, c);
      printf("\n");
    }
   
  return 0;
}
